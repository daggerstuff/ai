base_model: deepseek-ai/DeepSeek-V4-Pro
model_type: AutoModelForCausalLM
tokenizer_type: AutoTokenizer

load_in_8bit: false
load_in_4bit: true
strict: false

datasets:
  - path: s3://pixel-data/final_dataset/shards/curriculum/stage1/shard_000.jsonl
    type: chat_template
    chat_template: chatml
dataset_prepared_path: last_run_prepared
val_set_size: 0.05
output_dir: ./lora-out

adapter: qlora
lora_model_dir:

sequence_len: 2048
sample_packing: true
pad_to_sequence_len: true

lora_r: 16
lora_alpha: 32
lora_dropout: 0.1
lora_target_modules:
  - q_proj
  - v_proj
  - k_proj
  - o_proj
  - gate_proj
  - down_proj
  - up_proj

wandb_project: pixelated-empathy-kan28
wandb_entity:
wandb_watch:
wandb_name:
wandb_log_model:

gradient_accumulation_steps: 4
micro_batch_size: 4
num_epochs: 3
optimizer: paged_adamw_32bit
lr_scheduler: cosine

learning_rate: 0.0002
train_on_inputs: false
group_by_length: false
bf16: true
fp16: false
tf32: false

gradient_checkpointing: true
early_stopping_patience:
resume_from_checkpoint:
local_rank:
logging_steps: 10
xformers_attention:
flash_attention: true

warmup_steps: 100
eval_steps: 50
eval_table_size:
eval_table_max_new_tokens: 128
save_steps:
debug:
deepspeed: ai/training/configs/ds_config_zero3.json
weight_decay: 0.0
fsdp:
fsdp_config:
special_tokens:
  bos_token: '<|im_start|>'
  eos_token: '<|im_end|>'
  pad_token: '<|im_end|>'
