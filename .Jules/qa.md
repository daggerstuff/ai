## 2026-03-26 - Mocking TranscriptCorrector File I/O | Pattern: When instantiating utils.transcript_corrector.TranscriptCorrector in tests, avoid brittle tests or FileNotFoundError by explicitly mocking file I/O instead of passing dummy file paths (e.g., 'nonexistent.json'). Use @patch('utils.transcript_corrector.Path.exists', return_value=True) and @patch('builtins.open', new_callable=mock_open, read_data='{}') with a valid JSON string. | Action: Mock Path.exists and builtins.open when testing components that load JSON configs.
