## 2024-03-24 - Information Disclosure in API | Vulnerability: Raw Database Error Leak | Learning: The `dataset_api.py` was returning the raw `sqlite3.Error` string directly to the end user inside an `HTTPException(500)`. This leaks database schema and potential query structures. | Prevention: Always catch raw database errors and replace them with generic user-facing error messages like "An internal database error occurred."
