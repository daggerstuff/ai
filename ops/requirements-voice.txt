# Voice pipeline dependencies (whisperx)
# Install in a separate virtual environment:
#   uv venv .venv-voice
#   uv pip install -r requirements-voice.txt
#
# NOTE: whisperx requires huggingface-hub<1.0.0, which conflicts with
# the main project's transformers>=5.x requirement (huggingface-hub>=1.3.0).
# Hence this separate requirements file rather than a pyproject.toml group.

whisperx>=3.8.6
faster-whisper>=1.2.0
huggingface-hub<1.0.0
