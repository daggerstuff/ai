torch>=2.13.0
transformers>=5.14.1
datasets>=2.14.0
tokenizers>=0.14.0
accelerate>=0.24.0
peft>=0.6.0
bitsandbytes>=0.41.0
trl>=0.14
scipy>=1.11.0
scikit-learn>=1.3.0
pandas>=2.1.0
numpy>=1.24.0
polars>=0.19.0
pyarrow>=25.0.0
tqdm>=4.66.0
evaluate>=0.4.0
rouge-score>=0.1.2
bleu>=0.2.0
bert-score>=0.3.13
psutil>=5.9.0
gpustat>=1.1.0
rich>=13.6.0
typer>=0.9.0
pydantic>=2.4.0
omegaconf>=2.3.0

# Optional (guarded import in mental_ift_trainer.py): flash attention for long-context training.
# FA2 (2.x) is the CUDA-compiled line; FA4 (flash-attn-4) is the newer pure-Python CuTe DSL JIT line.
# Install the one matching the target GPU/CUDA toolchain:
#   pip install flash-attn           # FA2 (Hopper H100/Ampere A100)
#   pip install "flash-attn-4[cu13]" # FA4 (Blackwell B200, CUDA 13)
flash-attn>=2.7.0

# Security: Pin transitive dependencies to secure versions
urllib3>=2.7.0  # Fixes GHSA-gm62-xv2j-4w53, GHSA-2xpw-w6gg-jr37 (CVE-2025-66418, CVE-2025-66471) — 2.6.2 is the minimum that enforces the decompression bomb limit
