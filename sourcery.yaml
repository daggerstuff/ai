rules:
 - id: sqlalchemy-execute-raw-query
   description: Detects raw SQL execution via cursor.execute(), which may indicate SQL injection risk or inefficient query usage
   pattern: "cursor.execute(...)"